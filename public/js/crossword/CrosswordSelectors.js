class CrosswordSelectors {
    cells = '.cell';
    chars = '.char_for_enter';
    display = '#display';
    logWindow = '#log_window';
    additionalWords = '#additional_words';
    counterNotInCrosswordWord = '#counter_not_in_crossword_word';
    counterInCrosswordWord = '#counter_in_crossword_word';
}

var Selector = new CrosswordSelectors();
export default Selector;